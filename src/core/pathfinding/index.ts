/**
 * core/pathfinding — reliable navigation over the grid that copes with moving
 * agents (occupied tiles are blockers). A* for a single target, BFS-to-nearest
 * over a goal set, and the geometry helpers (manhattan, neighbours, directionTo)
 * the rest of the codebase shares. Pure functions over a map + blocker view.
 *
 * Intended files (added in Phase 2):
 *   - geometry.ts — manhattan, neighbours, directionTo, coord helpers (y inverted).
 *   - astar.ts    — A* to a single target tile.
 *   - bfs.ts      — BFS to the nearest tile in a goal set.
 */

export {};
